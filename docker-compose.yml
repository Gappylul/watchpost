services:
  watchpost:
    build: .
    ports:
      - "8080:8080"
      - "9090:9090"
    volumes:
      - ./watchpost.yml:/watchpost.yml
    command: ["/watchpost", "--config", "/watchpost.yml"]
    restart: unless-stopped